# Quartz status endpoint — nginx location snippet
# Include inside your server {} block that serves the Quartz site.
# Adjust alias path if your deployment differs from /srv/quartz.

location /status {
    alias /srv/quartz/public/status.json;
    default_type application/json;
    add_header Access-Control-Allow-Origin *;
}
